<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assetflow.mapper.UserBudgetMapper">

    <select id="findByUserId" resultType="UserBudget">
        SELECT id, user_id, category, monthly_limit
        FROM user_budget
        WHERE user_id = #{userId}
        ORDER BY category
    </select>

    <insert id="upsert" parameterType="UserBudget">
        INSERT INTO user_budget (user_id, category, monthly_limit)
        VALUES (#{userId}, #{category}, #{monthlyLimit})
        ON DUPLICATE KEY UPDATE monthly_limit = #{monthlyLimit}
    </insert>

    <delete id="deleteByUserIdAndCategory">
        DELETE FROM user_budget WHERE user_id = #{userId} AND category = #{category}
    </delete>

</mapper>
